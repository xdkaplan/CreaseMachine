<Window x:Class="PieceSolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PieceSolver"
        Title="PieceSolver" Height="700" Width="1000"
        Background="#1E1E1E">
    <Window.Resources>
        <!-- Reusable: remaps a slider's value within [min,max] to a 0-100% readout. -->
        <local:RangeToPercentConverter x:Key="RangeToPercent" />
        <!-- Shows a fraction directly as a percent (value × 100), allowing >100% (e.g. deCraze to 300%). -->
        <local:FractionToPercentConverter x:Key="FractionToPercent" />
        <!-- sidebar-collapse glyph (from sidebar-collapse.svg, 800x800); evenodd (F0). Mirrored
             with ScaleX=-1 for the right panel. -->
        <!-- Dark, flat tab headers (the default WPF TabItem renders light/3D, clashing with the
             hand-styled dark panels). Implicit style -> applies to every TabItem in the window. -->
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#8A8A98" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="hdr" Padding="9,4" Margin="0,0,2,0" Background="Transparent"
                                BorderThickness="0,0,0,2" BorderBrush="Transparent">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="hdr" Property="BorderBrush" Value="#6A9FE0" />
                                <Setter Property="Foreground" Value="#E0E0E8" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#C8C8D0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark menu styling (MenuItem / ContextMenu / in-menu Separator templates) now lives app-wide in App.xaml. -->


        <Geometry x:Key="CollapseIcon">F0 m375.35 316.42l-82.33 82.33 82.33 82.34c4.97 4.62 7.84 11.06 7.96 17.85 0.12 6.78-2.52 13.32-7.32 18.11-4.8 4.8-11.34 7.44-18.12 7.32-6.78-0.12-13.22-2.99-17.85-7.95l-100-100c-2.32-2.32-4.16-5.08-5.41-8.11-1.25-3.03-1.9-6.28-1.9-9.56 0-3.28 0.65-6.53 1.9-9.56 1.25-3.03 3.09-5.78 5.41-8.1l100-100c4.74-4.42 11.01-6.82 17.49-6.71 6.47 0.11 12.65 2.74 17.23 7.32 4.58 4.58 7.21 10.76 7.32 17.24 0.11 6.47-2.29 12.74-6.71 17.48z m125 66.67c-7.66 0-15.25 1.51-22.32 4.44-7.08 2.93-13.51 7.23-18.93 12.64-5.41 5.42-9.71 11.85-12.64 18.93-2.93 7.07-4.44 14.66-4.44 22.32v550c0 32.2 26.13 58.33 58.33 58.33h550c7.66 0 15.25-1.51 22.32-4.44 7.08-2.93 13.51-7.23 18.93-12.64 5.41-5.42 9.71-11.85 12.64-18.93 2.93-7.07 4.44-14.66 4.44-22.32v-550c0-7.66-1.51-15.25-4.44-22.32-2.93-7.08-7.23-13.51-12.64-18.93-5.42-5.41-11.85-9.71-18.93-12.64-7.07-2.93-14.66-4.44-22.32-4.44zm-8.33 58.33q0-1.66 0.63-3.19 0.64-1.53 1.81-2.7 1.17-1.17 2.7-1.81 1.53-0.63 3.19-0.63h375v566.66h-375q-1.66 0-3.19-0.63-1.53-0.64-2.7-1.81-1.17-1.17-1.81-2.7-0.63-1.53-0.63-3.19zm433.33 558.33v-566.66h125q1.66 0 3.19 0.63 1.53 0.64 2.7 1.81 1.17 1.17 1.81 2.7 0.63 1.53 0.63 3.19v550q0 1.66-0.63 3.19-0.64 1.53-1.81 2.7-1.17 1.17-2.7 1.81-1.53 0.63-3.19 0.63z</Geometry>
        <!-- Left tool-rail icons (open-source SVG paths; F1 = nonzero fill, matching the source). -->
302-0.896-0.458-1.395-0.458c-0.291,0-0.586,0.053-0.87,0.162l-0.338,0.13c-0.193,0.072-0.402-0.003-0.503-0.178l-0.567-0.984c-0.102-0.176-0.062-0.396,0.097-0.524l0.276-0.224c0.634-0.516,0.961-1.312,0.873-2.132C6.012,12.422,6,12.212,6,12c0-0.212,0.012-0.421,0.034-0.63c0.088-0.819-0.239-1.615-0.873-2.13L4.885,9.016C4.728,8.889,4.687,8.668,4.788,8.492l0.567-0.983C5.457,7.332,5.67,7.259,5.858,7.33l0.338,0.129c0.768,0.295,1.614,0.183,2.263-0.294C8.816,6.902,9.197,6.68,9.596,6.505c0.731-0.323,1.245-0.999,1.374-1.806l0.057-0.354C11.059,4.145,11.229,4,11.433,4h1.135c0.203,0,0.374,0.145,0.406,0.348l0.057,0.35c0.129,0.809,0.643,1.484,1.376,1.808c0.397,0.175,0.779,0.396,1.134,0.658c0.648,0.479,1.493,0.591,2.264,0.296l0.338-0.13c0.191-0.073,0.402,0.003,0.503,0.178l0.567,0.984c0.102,0.176,0.062,0.396-0.098,0.524L18.839,9.24c-0.634,0.516-0.961,1.312-0.873,2.132C17.988,11.578,18,11.788,18,12c0,0.212-0.012,0.421-0.034,0.629c-0.088,0.818,0.239,1.614,0.874,2.13l0.277,0.225C19.274,15.112,19.314,15.332,19.213,15.508z</Geometry>

        <!-- Fold-gutter (the thin tab a collapsed pane shows): dark with a hover wash. -->
        <Style x:Key="FoldGutter" TargetType="Border">
            <Setter Property="Background" Value="#252526" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2E2E33" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Flat top-bar button (icon + text), no WPF chrome — transparent with a hover wash. -->
        <Style x:Key="FlatBarButton" TargetType="Button">
            <Setter Property="Foreground" Value="#C8C8C8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3" Padding="8,3">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#3A3A40" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#45454C" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <!-- Standard 4-panel docked chrome: Windows-style menu bar, fixed top/bottom bars, drag-resizable
         + collapsible left/right panels (chevron + drag-below-32px), center hosts the GL viewport. -->
    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <!-- MENU bar (Windows-style). Shortcuts shown via InputGestureText are bound in code-behind. -->
        <Menu x:Name="MenuBar" Grid.Row="0" Background="#2D2D30" Foreground="#C8C8C8" Padding="2,1">
            <MenuItem Header="_File">
                <MenuItem x:Name="MenuImport" Header="_Import…" />
                <Separator />
                <MenuItem x:Name="MenuRevert" Header="_Revert" InputGestureText="Ctrl+R" />
            </MenuItem>
            <MenuItem Header="_Window">
                <MenuItem x:Name="MenuConsole" Header="_Console" IsCheckable="True" InputGestureText="Ctrl+Shift+J" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem x:Name="MenuAbout" Header="_About" />
            </MenuItem>
        </Menu>

        <!-- TOP bar (full width, 32px) -->
        <Border x:Name="TopBar" Grid.Row="1" Background="#2D2D30">
            <DockPanel LastChildFill="False" Margin="8,0">
                <TextBlock DockPanel.Dock="Left" Text="PieceSolver" Foreground="#C8C8C8"
                           VerticalAlignment="Center" FontSize="12" />
                <!-- Right-docked: first declared sits rightmost. Layout L->R: Propose | Solve | Settings.
                     (Revert moved to File > Revert. Subdivision is folded into Solve via its slider — no separate button.) -->
                <Button x:Name="SettingsButton" DockPanel.Dock="Right" Style="{StaticResource FlatBarButton}"
                        Margin="6,0,0,0" VerticalAlignment="Center" ToolTip="Settings (placeholder — not wired yet)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="15" Height="15" VerticalAlignment="Center">
                            <Path Data="{StaticResource IconSettings}" Fill="#C8C8C8" />
                        </Viewbox>
                        <TextBlock Text="Settings" Margin="6,0,0,0" FontSize="12" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button x:Name="SolveButton" DockPanel.Dock="Right" Content="Solve"
                        Padding="16,2" Margin="4,0,0,0" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"
                        ToolTip="Bake: develop to the selected Accuracy, then for each Subdivision level subdivide and re-develop. Runs on a worker with a progress + cancel modal, then shows the result." />
                <Button x:Name="ProposeButton" DockPanel.Dock="Right" Content="Propose"
                        Padding="12,2" Margin="4,0,0,0" FontSize="12" VerticalAlignment="Center"
                        ToolTip="Stage 2: run the developability flow on a copy of the mesh (no collapse) and propose piece-boundary creases at edges whose fold angle ≥ Crease ∠. Runs behind the progress modal, then opens a camera-only review (full patchwork + live Crease ∠) to Accept or discard." />
            </DockPanel>
        </Border>

        <!-- MIDDLE row: left panel | splitter | center | splitter | right panel -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftCol" Width="220" MinWidth="26" />
                <ColumnDefinition x:Name="LeftSplitCol" Width="6" />
                <ColumnDefinition Width="*" MinWidth="120" />
                <ColumnDefinition x:Name="RightSplitCol" Width="6" />
                <ColumnDefinition x:Name="RightCol" Width="240" MinWidth="26" />
            </Grid.ColumnDefinitions>

            <!-- LEFT panel: placeholder (the session log now lives in the toggleable Console window;
                 Window > Console or Ctrl+Shift+J). Collapse chevron at the top-RIGHT (inner) corner. -->
            <Border x:Name="LeftPanel" Grid.Column="0" Background="#252526" ClipToBounds="True">
                <Grid>
                    <Button x:Name="LeftCollapseBtn" HorizontalAlignment="Right" VerticalAlignment="Top"
                            Width="24" Height="24" Margin="0,4,4,0" Background="Transparent"
                            BorderThickness="0" Cursor="Hand" ToolTip="Collapse / expand panel">
                        <Viewbox Width="15" Height="15">
                            <Path Data="{StaticResource CollapseIcon}" Fill="#A0A6AE" />
                        </Viewbox>
                    </Button>
                </Grid>
            </Border>

            <!-- LEFT splitter (drag to resize, click to collapse/expand) -->
            <GridSplitter x:Name="LeftSplitter" Grid.Column="1" Width="6"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          Background="#3F3F46" ShowsPreview="False"
                          ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" />

            <!-- CENTER: 3D viewport | folding divider | 2D layout. Drag the divider to an edge to stash a pane
                 into its gutter tab; click the tab to pop it back to its remembered width. GL injects into CenterHost. -->
            <Grid x:Name="SplitGrid" Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="Col3D" Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition x:Name="Col2D" Width="*" />
                </Grid.ColumnDefinitions>
                <!-- 3D pane (GL viewport + brush preview inject into CenterHost in code-behind — name preserved) -->
                <Grid Grid.Column="0">
                    <Grid x:Name="CenterHost" Background="#1A1A1C" ClipToBounds="True" />
                    <Border x:Name="Gutter3D" Visibility="Collapsed" Cursor="Hand" Style="{StaticResource FoldGutter}" ToolTip="Show 3D">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource IconModel}" Fill="#6A9FE0" /></Viewbox>
                            <TextBlock Text="3D" FontSize="10" Foreground="#6A9FE0" HorizontalAlignment="Center" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </Grid>
                <!-- folding divider (only visible when both panes are open) -->
                <Border x:Name="CenterDivider" Grid.Column="1" Width="8" Background="#303034" Cursor="SizeWE" />
                <!-- 2D pane (placeholder for now) -->
                <Grid Grid.Column="2">
                    <Border x:Name="View2DPane" Background="#202024">
                        <TextBlock Text="2D LAYOUT" Foreground="#54545E" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <Border x:Name="Gutter2D" Visibility="Collapsed" Cursor="Hand" Style="{StaticResource FoldGutter}" ToolTip="Show 2D">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource IconLayout}" Fill="#6A9FE0" /></Viewbox>
                            <TextBlock Text="2D" FontSize="10" Foreground="#6A9FE0" HorizontalAlignment="Center" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <!-- RIGHT splitter (drag to resize, click to collapse/expand) -->
            <GridSplitter x:Name="RightSplitter" Grid.Column="3" Width="6"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          Background="#3F3F46" ShowsPreview="False"
                          ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" />

            <!-- RIGHT panel: SIMULATION / DISPLAY tabs (bound to SimSettings view-model). Collapse
                 chevron sits in its own thin strip at the top-LEFT (inner) corner, icon mirrored;
                 the tab strip + content fill the rest. -->
            <Border x:Name="RightPanel" Grid.Column="4" Background="#2A2A2E" ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="26" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- chevron strip -->
                    <Button x:Name="RightCollapseBtn" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top"
                            Width="24" Height="24" Margin="4,2,0,0" Background="Transparent"
                            BorderThickness="0" Cursor="Hand" ToolTip="Collapse / expand panel">
                        <Viewbox Width="15" Height="15">
                            <Path Data="{StaticResource CollapseIcon}" Fill="#A0A6AE"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <ScaleTransform ScaleX="-1" />
                                </Path.RenderTransform>
                            </Path>
                        </Viewbox>
                    </Button>

                    <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0">
                        <TabItem Header="SIMULATION">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,8,0,8">
                                <StackPanel Margin="10,0,10,0">
                                    <!-- Test-asset picker: Source set (Solids/Surfaces/Meshes) + a 1..30 index
                                         within it. Changing either resets the app to that mesh (load + clear
                                         flat + re-arm BFF). -->
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Source" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Which test-asset set the Mesh index picks from. Solids = 6-sided FBX (unwelded into per-face components); Surfaces = NURBS single open-patch STLs; Meshes = arbitrary triangle meshes (currently just the bunny)." />
                                        <ComboBox SelectedIndex="{Binding AssetSet, Mode=TwoWay}" FontSize="11" VerticalAlignment="Center" Margin="6,0">
                                            <ComboBoxItem Content="Solids" />
                                            <ComboBoxItem Content="Surfaces" />
                                            <ComboBoxItem Content="Meshes" />
                                        </ComboBox>
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Mesh" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Index within the selected Source set (1-30). Switching resets the solver to that mesh. (Meshes: 1 = Bunny, 2 = Unwelded; out-of-range clamps to the last.)" />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding MeshIndex}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="1" Maximum="30" Value="{Binding MeshIndex}" IsSnapToTickEnabled="True" TickFrequency="1" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>

                                    <!-- Crease ∠ lives in the post-Propose camera-modal review now (not here). -->
                                    <Separator Margin="0,8,0,2" Background="#3F3F46" />
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Subdiv" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Subdivision level: how many 1->4 refine rounds Solve does (develop to accuracy, then subdivide + re-develop, per level). 0 = no subdivision." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding SubdivLevel}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="4" Value="{Binding SubdivLevel}" IsSnapToTickEnabled="True" TickFrequency="1" TickPlacement="BottomRight" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>

                                    <!-- GO workflow: Accuracy (material) + Tolerance (fit-up mm) up top; raw solver
                                         terms fold under Advanced. Startup config = preset D (single developable). -->
                                    <DockPanel Margin="0,8,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Accuracy" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="How developable it must be, by material: Fabric (stretchy) -> Paper -> Sheet Metal -> Plate Metal (stiff, near-perfect). Sets the allowable in-plane strain." />
                                        <TextBlock DockPanel.Dock="Right" Width="80" Text="{Binding AccuracyLabel}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="3" Value="{Binding AccuracyLevel}" IsSnapToTickEnabled="True" TickFrequency="1" TickPlacement="BottomRight" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,2,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Strain" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Live in-plane strain (relative edge-length error = developability). GO when it is within the selected material's allowable strain." />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding GoText}" Foreground="{Binding GoBrush}" FontSize="11" FontWeight="Bold" Width="80" TextAlignment="Right" VerticalAlignment="Center" Margin="6,0" />
                                        <TextBlock Text="{Binding StrainText}" Foreground="#8A8A98" FontSize="11" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Tolerance" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Absolute fit-up tolerance (gap between adjacent panels at assembly). Used later for panel sizing / acceptance." />
                                        <TextBlock DockPanel.Dock="Right" Width="80" Text="{Binding ToleranceMm, StringFormat={}{0:0.##} mm}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="10" Value="{Binding ToleranceIndex}" IsSnapToTickEnabled="True" TickFrequency="1" TickPlacement="BottomRight" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="" Width="62" />
                                        <CheckBox Content="Fix B-spline edges (pin seams to a bent wire)" IsChecked="{Binding FixBSplineEdges, Mode=TwoWay}" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center" Margin="6,0"
                                                  ToolTip="Snap each patch boundary onto a low-DOF degree-3 B-spline (~1 control point per 5 mesh points) and hold it fixed while solving. Seams become smooth shared wires; cyan overlay shows the fitted curve. Takes effect on the next Solve." />
                                    </DockPanel>

                                    <Expander Header="Advanced" IsExpanded="False" Foreground="#9AA0A8" FontSize="11" Margin="0,10,0,0">
                                        <StackPanel Margin="0,6,0,0">
                                            <DockPanel Margin="0,0,0,2">
                                                <TextBlock DockPanel.Dock="Left" Text="Preset" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                           ToolTip="Raw developability presets. A scale-pin only · B +anchor (low-drift) · C +differential bending (faithful+smooth) · D no-anchor+plain-bending (single entirely-developable patch = the default)." />
                                                <StackPanel Orientation="Horizontal">
                                                    <Button x:Name="PresetAButton" Content="A" Width="30" Margin="2,0" FontSize="11" ToolTip="A — scale-pin only (wrinkly, more drift)" />
                                                    <Button x:Name="PresetBButton" Content="B" Width="30" Margin="2,0" FontSize="11" ToolTip="B — low-drift: + small anchor" />
                                                    <Button x:Name="PresetCButton" Content="C" Width="30" Margin="2,0" FontSize="11" ToolTip="C — low-drift + smooth: anchor + differential bending" />
                                                    <Button x:Name="PresetDButton" Content="D" Width="30" Margin="2,0" FontSize="11" ToolTip="D — single entirely-developable patch (no anchor + plain bending) = the default" />
                                                </StackPanel>
                                            </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Iso" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Developability driver (gain): weight on equalizing corresponding edge lengths between M and the flat M'. LM is stable at any value — crank it freely." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding IsoWeight, StringFormat=0.##}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="50" Value="{Binding IsoWeight}" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Fair" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Fairness/smoothing weight. With 'Differential' (below) ON it de-buckles without shrinking (preserves original detail) — the C preset. OFF it's a plain Laplacian (shrinks); leave low or 0." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding FairWeight, StringFormat=0.##}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="1" Value="{Binding FairWeight}" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,1,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="" Width="62" />
                                        <CheckBox Content="Differential (non-shrinking)" IsChecked="{Binding DiffFair, Mode=TwoWay}" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center" Margin="6,0"
                                                  ToolTip="Fair mode. On: penalize the CHANGE in local shape from the original (non-shrinking, preserves detail). Off: plain Laplacian (shrinks)." />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Anchor" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Faithfulness dial: weight holding M near its original shape. Lower toward 0 to drive relErr -> ~0 (more deformation); raise to stay faithful. Without any anchor M can collapse flat onto M'." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding AnchorWeight, StringFormat=0.##}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="2" Value="{Binding AnchorWeight}" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Bend" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="2nd-order bending (differential bi-Laplacian) — the ANTI-WRINKLE term (paper's 2nd-difference fairness, triangle analog). Penalises change in curvature from the original, flattening buckles WITHOUT shrinking. Raise to de-wrinkle (preset C uses 0.6); very high slightly fights developability." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding BendWeight, StringFormat=0.##}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="3" Value="{Binding BendWeight}" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Scale" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Global scale pin (anti-collapse): pins total size to the original so M can't shrink flat. This single global term is all that's needed in place of a dense Anchor — with Scale on, Anchor can be 0 and M still develops to relErr ~0." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding ScaleWeight, StringFormat=0.##}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="50" Value="{Binding ScaleWeight}" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Smoother" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Non-shrinking smoother applied after each LM step (boundary pinned): None / Tangential (slides vertices in-surface) / Taubin (lambda-mu, no net shrink). Distributes strain / de-buckles. None suffices for near-developable inputs." />
                                        <ComboBox SelectedIndex="{Binding SmoothKind, Mode=TwoWay}" Height="20" FontSize="11" VerticalAlignment="Center" Margin="6,0">
                                            <ComboBoxItem Content="None" />
                                            <ComboBoxItem Content="Tangential" />
                                            <ComboBoxItem Content="Taubin" />
                                        </ComboBox>
                                    </DockPanel>
                                    <DockPanel Margin="0,3,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="Smooth" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                   ToolTip="Smoother strength (only active when Smoother is not None)." />
                                        <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding SmoothStrength, StringFormat=0.##}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                        <Slider Minimum="0" Maximum="1" Value="{Binding SmoothStrength}" VerticalAlignment="Center" Margin="6,0" />
                                    </DockPanel>
                                        </StackPanel>
                                    </Expander>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <TabItem Header="DISPLAY">
                            <StackPanel Margin="10,12,10,0">
                                <TextBlock Text="SHADING" Foreground="#7A7A88" FontSize="10" FontWeight="Bold" Margin="2,0,0,6" />
                                <DockPanel Margin="0,3,0,0">
                                    <TextBlock DockPanel.Dock="Left" Text="Facet" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                               ToolTip="0% = smooth (averaged normals) · 100% = faceted (per-face). Shader-blended, live." />
                                    <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding Facet, Converter={StaticResource FractionToPercent}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                    <Slider Minimum="0" Maximum="1" Value="{Binding Facet}" VerticalAlignment="Center" Margin="6,0" />
                                </DockPanel>
                                <DockPanel Margin="0,3,0,0">
                                    <TextBlock DockPanel.Dock="Left" Text="Curve" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                               ToolTip="Facet response exponent: blend = Facet^Curve. 1 = linear; higher keeps facets soft until high Facet, then sharpens." />
                                    <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding FacetExp, StringFormat=0.#}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                    <Slider Minimum="1" Maximum="30" Value="{Binding FacetExp}" VerticalAlignment="Center" Margin="6,0" />
                                </DockPanel>
                                <DockPanel Margin="0,3,0,0">
                                    <TextBlock DockPanel.Dock="Left" Text="Shine" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                               ToolTip="Default shading: blend from neutral matte light (0%) to the reflective environment map (100%)." />
                                    <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding Shine, Converter={StaticResource FractionToPercent}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                    <Slider Minimum="0" Maximum="1" Value="{Binding Shine}" VerticalAlignment="Center" Margin="6,0" />
                                </DockPanel>
                                <DockPanel Margin="0,6,0,0">
                                    <TextBlock DockPanel.Dock="Left" Text="Inset" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                               ToolTip="Piece-view seam width (border / bevel-lip / gap) as a fraction of mesh size — world-relative, stable under zoom. Lower = thinner. Live." />
                                    <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding InsetWidthFrac, Converter={StaticResource FractionToPercent}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                    <Slider Minimum="0.003" Maximum="0.12" Value="{Binding InsetWidthFrac}" VerticalAlignment="Center" Margin="6,0" />
                                </DockPanel>

                                <TextBlock Text="RULING LINES" Foreground="#7A7A88" FontSize="10" FontWeight="Bold" Margin="2,16,0,6" />
                                <CheckBox Content="Show ruling lines (curvature grain)" IsChecked="{Binding ShowRuling, Mode=TwoWay}" Foreground="#C8C8C8" FontSize="11" Margin="2,0,0,0"
                                          ToolTip="Curvature-driven LIC grain on M (the developable 'grain') modulating the matcap. Flat areas stay bare; tightly-curved areas show wind-swept hairs. Combed = developable, swirly = doubly-curved." />
                                <Expander Header="Advanced" IsExpanded="False" Foreground="#9AA0A8" FontSize="11" Margin="0,6,0,0">
                                    <StackPanel Margin="0,6,0,0">
                                        <DockPanel Margin="0,0,0,2">
                                            <TextBlock DockPanel.Dock="Left" Text="Grain" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                       ToolTip="LIC grain fineness (both modes): noise tiles across the model. Higher = finer, crisper hairs. Live, no recompile." />
                                            <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding LicGrain, StringFormat={}{0:0}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                            <Slider Minimum="2" Maximum="40" Value="{Binding LicGrain}" VerticalAlignment="Center" Margin="6,0" />
                                        </DockPanel>
                                        <DockPanel Margin="0,3,0,0">
                                            <TextBlock DockPanel.Dock="Left" Text="Length" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                       ToolTip="LIC streak length — the high-confidence max. On Ruling, confidence scales it down: short specks in poorly-ruled (doubly-curved) areas, long wind-swept hairs where a ruling is crisp. Live." />
                                            <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding LicLength, StringFormat={}{0:0}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                            <Slider Minimum="2" Maximum="40" Value="{Binding LicLength}" IsSnapToTickEnabled="True" TickFrequency="1" VerticalAlignment="Center" Margin="6,0" />
                                        </DockPanel>
                                        <DockPanel Margin="0,3,0,0">
                                            <TextBlock DockPanel.Dock="Left" Text="Alpha" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                       ToolTip="LIC grain alpha — the high-confidence max opacity. On Ruling, confidence scales it down: faint specks in poorly-ruled areas, bold hairs where a ruling is crisp. Live." />
                                            <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding LicAlpha, StringFormat={}{0:0.00}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                            <Slider Minimum="0" Maximum="1" Value="{Binding LicAlpha}" VerticalAlignment="Center" Margin="6,0" />
                                        </DockPanel>
                                        <DockPanel Margin="0,3,0,0">
                                            <TextBlock DockPanel.Dock="Left" Text="Curv min" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                       ToolTip="Ruling curvature remap (low falloff): max curvature at/below this reads as flat — no hairs. Raise to ignore more gentle curvature (treat more of the surface as flat). Live." />
                                            <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding LicCurvMin, StringFormat={}{0:0.00}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                            <Slider Minimum="0" Maximum="1" Value="{Binding LicCurvMin}" VerticalAlignment="Center" Margin="6,0" />
                                        </DockPanel>
                                        <DockPanel Margin="0,3,0,0">
                                            <TextBlock DockPanel.Dock="Left" Text="Curv max" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center"
                                                       ToolTip="Ruling curvature remap (high falloff): max curvature at/above this reads as fully curved — long bold hairs. Lower it to make hairs appear at gentler curvature. Live." />
                                            <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding LicCurvMax, StringFormat={}{0:0.00}}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                            <Slider Minimum="0" Maximum="1" Value="{Binding LicCurvMax}" VerticalAlignment="Center" Margin="6,0" />
                                        </DockPanel>
                                    </StackPanel>
                                </Expander>

                                <TextBlock Text="ADVANCED" Foreground="#7A7A88" FontSize="10" FontWeight="Bold" Margin="2,16,0,6" />
                                <CheckBox Content="Use Matcap" IsChecked="{Binding UseMatcap, Mode=TwoWay}" Foreground="#C8C8C8" FontSize="11" Margin="2,0,0,6"
                                          ToolTip="Override the neutral + Shine shading with a hand-picked matcap from the set below." />
                                <ListBox x:Name="MatcapList" Background="Transparent" BorderThickness="0" Padding="0"
                                         IsEnabled="{Binding UseMatcap}"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border x:Name="bd" BorderThickness="2" BorderBrush="Transparent" CornerRadius="3" Margin="3">
                                                            <ContentPresenter />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="bd" Property="BorderBrush" Value="#4A5A70" />
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="bd" Property="BorderBrush" Value="#6A9FE0" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Image Source="{Binding}" Width="46" Height="46" Stretch="UniformToFill" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </TabItem>

                        <TabItem Header="BRUSH">
                            <StackPanel Margin="10,12,10,0">
                                <TextBlock Text="CREASE BRUSH" Foreground="#7A7A88" FontSize="10" FontWeight="Bold" Margin="2,0,0,6" />
                                <DockPanel Margin="0,6,0,0">
                                    <TextBlock DockPanel.Dock="Left" Text="Size" Width="62" Foreground="#C8C8C8" FontSize="11" VerticalAlignment="Center" ToolTip="Brush size: 10 notches (Fibonacci world radii 0.1 … 8.9). 5 (default) = 0.8; low notches fine, high notches grow fast. Also [ / ]." />
                                    <TextBlock DockPanel.Dock="Right" Width="40" Text="{Binding BrushSize, StringFormat=0}" Foreground="#8A8A98" FontSize="11" TextAlignment="Right" VerticalAlignment="Center" />
                                    <Slider Minimum="1" Maximum="10" Value="{Binding BrushSize}" IsSnapToTickEnabled="True" TickFrequency="1" VerticalAlignment="Center" Margin="6,0" />
                                </DockPanel>
                                <TextBlock Text="After Accept the brush is live on the model (no toggle): left-click a patch to select it (light blue), drag across a seam to grow it. Shift+click starts a NEW piece (e.g. a bullseye inside a patch). Right-drag orbits. Re-Propose to redo the angle." Foreground="#6E6E78" FontSize="10" TextWrapping="Wrap" Margin="2,12,0,0" />
                            </StackPanel>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>

        <!-- BOTTOM bar (full width, 32px): live perf/status readout. Session transport is in the Console. -->
        <Border x:Name="BottomBar" Grid.Row="3" Background="#2D2D30">
            <DockPanel Margin="8,0">
                <TextBlock x:Name="StatusText" DockPanel.Dock="Right" Text="" Foreground="#8A8A98"
                           VerticalAlignment="Center" FontSize="11" Margin="8,0,0,0" />
            </DockPanel>
        </Border>

        <!-- Modal bake overlay: shown while Solve runs on a background worker (progress + cancel). -->
        <Border x:Name="BakeOverlay" Grid.Row="0" Grid.RowSpan="4" Background="#A6000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="4" Padding="22"
                    Width="340" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock x:Name="BakeStatus" Text="Solving…" Foreground="#E8E8E8" FontSize="13" Margin="0,0,0,12" TextWrapping="Wrap" />
                    <ProgressBar x:Name="BakeBar" Height="16" Minimum="0" Maximum="100" Value="0" />
                    <Button x:Name="BakeCancel" Content="Cancel" Width="92" HorizontalAlignment="Right" Margin="0,14,0,0" Padding="8,3" />
                </StackPanel>
            </Border>
        </Border>

        <!-- Reusable CAMERA-ONLY modal: a floating card over the viewport. While it is up the rest of the
             chrome is disabled (SetChromeEnabled), but the viewport keeps orbit/pan/zoom. Title + body are set
             per use via ShowCamModal(); Accept/Cancel route to the supplied callbacks. Sits below BakeOverlay. -->
        <Border x:Name="CamModalBar" Grid.Row="2" Visibility="Collapsed" Panel.ZIndex="90"
                HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,28">
            <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="6" Padding="16,12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="14" ShadowDepth="2" Opacity="0.5" />
                </Border.Effect>
                <StackPanel MinWidth="320">
                    <TextBlock x:Name="CamModalTitle" Foreground="#E8E8E8" FontSize="12" FontWeight="Bold" Margin="0,0,0,10" />
                    <ContentControl x:Name="CamModalBody" />
                    <DockPanel LastChildFill="False" Margin="0,12,0,0">
                        <Button x:Name="CamModalAccept" DockPanel.Dock="Right" Content="Accept" MinWidth="84" Padding="10,4" Margin="6,0,0,0" FontWeight="Bold" />
                        <Button x:Name="CamModalCancel" DockPanel.Dock="Right" Content="Cancel" MinWidth="84" Padding="10,4" />
                    </DockPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
